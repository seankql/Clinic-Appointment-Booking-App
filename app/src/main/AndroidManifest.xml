<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.b07project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.B07Project">
        <activity android:name=".PreviouslyVisitedDoctors"></activity>
        <activity android:name=".DoctorScreen" />
        <activity android:name=".DisplayDoctorLogin" />
        <activity android:name=".PatientScreen" />
        <activity android:name=".DisplayPatientLogin" />
        <activity android:name=".patient_login2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>